services:
  - type: web
    name: server-error-log-manager
    env: java
    buildCommand: mvn clean package -q
    startCommand: java -jar target/server-error-log-manager-1.0-jar-with-dependencies.jar
    envVars:
      - key: PORT
        value: 8080
